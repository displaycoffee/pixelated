/* Local styles */
import './styles/scores.scss';

export const Scores = (props: ObjectPrimitiveProps) => {
	return (
		<div className="scores spacing-reset">
			<h2>Scores</h2>

			<p>this is the about page.</p>
		</div>
	);
};
