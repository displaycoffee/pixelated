/* Local styles */
import './styles/rules.scss';

export const Rules = (props: ObjectPrimitiveProps) => {
	return (
		<div className="rules spacing-reset">
			<h2>Rules</h2>

			<p>this is the rules page.</p>
		</div>
	);
};
